import React from 'react';
import Layout from '../Components/Layout';

const Admissions = () => (
    <Layout>
      <section className="announcement">
        <h2>Admissions</h2>
        <p>Guidelines and details on admission criteria, procedures, deadlines, and contact info for the admissions office.</p>
      </section>
    </Layout>
  );
  
  export default Admissions;
