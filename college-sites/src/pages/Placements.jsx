import React from 'react';
import Layout from '../Components/Layout';

const Placements = () => (
    <Layout>
      <section className="announcement">
        <h2>Placements</h2>
        <p>Information on placement statistics, career services, recruiters, internships, and training programs.</p>
      </section>
    </Layout>
  );
  
  export default Placements;
